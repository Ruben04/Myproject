{% load staticfiles %}
<html>
	<head>
		<title>Ligaplusapp</title>
		<link rel="stylesheet" type="text/css" href="{% static "css/recetas.css" %}">
	</head>
	<body>
		{% if user.is_authenticated %}
			<p>Soy {{ user.get_username }}</p>
			
		{% endif %}
		<nav>
			<ul>
				{% if user.is_authenticated %}	
					<li><a href="{% url 'logout' %}">Logout</a></li>
				{% else %}
					<li><a href="{% url 'registro' %}">Registro</a></li>
					<li><a href="{% url 'login' %}">Login</a></li>
				{% endif %}
			</ul>
		</nav>
		{% if lista_jugadores %}
			<table border="2">
				<tr>
					<td>Nombre</td>
					<td>Equipo</td>
					<td>Nacimiento</td>
				</tr>
				{% for jugadores in lista_jugadores %}
					<tr>
						<td>{{ jugadores.Nombre }}</td>
						<td>{{ jugadores.Equipo }}</td>
						<td>{{ jugadores.Nacimiento|date:"D d M Y - H:i" }}</td>
					</tr>
				{% endfor %}
			</table>
		{% else %}
			<p>No hay jugadores</p>			
		{% endif %}
<a href="{% url 'indice' %}">Volver</a> 
	</body>
</html>
